{
  "name": "focusplease",
  "version": "1.0.0",
  "private": true,
  "description": "Omarchy service plugin: focused Hyprland window claims its configured pixel size",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "omarchy plugin validate ."
  }
}
